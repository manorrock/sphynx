name: docker-snapshot
on:
  schedule:
  - cron: "0 12 * * *"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout sources
      uses: actions/checkout@v1
    - name: Set up JDK 1.8
      uses: actions/setup-java@v1
      with:
        java-version: 1.8
    - name: Build with Maven
      run: mvn -B -DskipTests=true -f pom.xml clean install
    - name: Login to Docker Hub
      uses: azure/docker-login@v1
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}
    - name: Build amd64 image
      run: |
        cd webapp
        docker build -f src/main/docker/Dockerfile -t collectivae/collectivae:snapshot .
        docker push collectivae/collectivae:snapshot
    - name: Build ARM32v6 image
      run: |
        cd webapp
        docker build -f src/main/docker/Dockerfile.arm32v6 -t collectivae/collectivae:arm32v6-snapshot .
        docker push collectivae/collectivae:arm32v6-snapshot
